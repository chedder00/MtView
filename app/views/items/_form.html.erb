<% if @item.order.nil? %>
  <%= render 'items/task_item', object: @task, locals: { item: item } %>
<% else %>
  <%= render 'items/order_item', object: @order, 
                                 locals: { item: item, products: @avaliable } %>
<% end %>

