<% render 'shared/page_title' %>
<h1><%= "#{@page_heading}" %></h1>

<%= render 'shared/paginate_list', object: @inventory_items , 
                                   locals: { class_name: "index", 
                                             display: "both"} %>